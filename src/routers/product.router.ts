// import { Router } from "express";
// import productController from '../controllers/product.controller'

// const router = Router()

// PUBLIC DATA -> bisa diakses siapa aja

// router.get('/', productController.findAll)
// router.get('/:id', productController.findById)

// SIMULASI RBAC -> hanya admin yang boleh akses CUD

// router.post('/', authMiddleware, roleMiddleware, productController.create)
// router.put('/:id', authMiddleware, roleMiddleware, productController.update)
// router.delete('/:id', authMiddleware, roleMiddleware, productController.delete)
